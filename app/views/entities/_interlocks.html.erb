<% cache(['interlocks', @entity.cache_key,  params[:page] ], expires_in: 2.days) do %>
  <%= render partial: 'connections',
             locals: { connection_type: :interlocks,
                       connected_entities: @entity.interlocks(params[:page]),
		       has_stat: false } %>
<% end  %>
