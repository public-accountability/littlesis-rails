<%#=
locals: bulletin

This partial is the html of the actual bulletin, used
on the home dashboard

dashboard_panel() in application_helper generates the
wrapping html for the bulletin.
%>

<%= dashboard_panel(heading: bulletin.title, color: bulletin.display_color) do %>
  <%= bulletin.content %>
<% end %>
