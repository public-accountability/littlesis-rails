<div id="entity-deletion-request-form">
  <%= form_tag(deletion_requests_entities_path, method: :post, class: 'user-request-form') do %>
    <%= hidden_field_tag 'entity_id', @entity.id %>
    <div class="entity-deletion-justification user-request-justification">
      <p>Please briefly explain why this entity should be deleted:</p>
    </div>
    <div id="entity-deletion-action-buttons">
      <%= text_area_tag 'justification', nil, size: "25x4", required: true %>
      <%= submit_tag 'Request Deletion', class: 'btn btn-lrg btn-primary', id: 'entity-deletion-request-btn' %>
    </div>
  <% end %>
</div>
