<div id="entity-deletion-action-buttons">

  <% submit_target = review_deletion_request_path(@deletion_request) %>

  <%= form_tag(submit_target, method: :post, class: 'entity_deletion-form') do %>
    <%= hidden_field_tag 'id', @deletion_request.id %>
    <%= hidden_field_tag 'decision', 'approved' %>
    <%= submit_tag 'Approve', class: 'btn btn-lrg btn-primary' %>
  <% end %>

  <%= form_tag(submit_target, method: :post, class: 'entity-deletion-form') do %>
    <%= hidden_field_tag 'id', @deletion_request.id %>
    <%= hidden_field_tag 'decision', 'denied' %>
    <%= submit_tag 'Deny', class: 'btn btn-lrg btn-danger' %>
  <% end %>

</div>
